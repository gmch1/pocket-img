# Changelog

## 0.4.6 - 2026-08-13

### Changed

- 新安装的 macOS 客户端默认全局快捷键改为 `F1`；升级时继续保留用户已经保存的快捷键配置。
- 刷新截图浮层样式：选区使用更清晰的双层描边，尺寸标签显示 Retina 实际像素，红框和箭头增加暗色轮廓，工具栏重新分组并突出最右侧上传按钮。
- 选区阶段增加指针像素放大预览、十字准星、像素坐标和十六进制颜色值。
- 标注工具栏增加 `A` 文本工具：选择后点击截图位置输入文字，按回车确认，最终文字会合成进上传图片。
- 删除早期 `UserDefaults` 配置迁移代码，客户端只从固定的 `settings.json` 读取和保存配置。

## 0.4.5 - 2026-08-13

### Changed

- GitHub Release 改用固定的长期自签名证书，不再为每次构建生成新的 ad-hoc 代码身份；从后续版本开始，macOS 可以把覆盖安装识别为同一应用更新。
- Release 工作流强制校验证书指纹、签名 Authority、Bundle ID 指定要求和非 ad-hoc 状态；签名 Secret 缺失或身份变化时拒绝发布。

### Security

- 私钥只保存在加密的本机备份和 GitHub Actions Secrets 中，仓库仅提交公开证书；应用继续不包含钥匙串访问代码或相关 entitlement。

## 0.4.4 - 2026-08-13

### Fixed

- 禁止 macOS 自动终止常驻菜单栏进程，并将截图覆盖窗口的释放推迟到鼠标事件回调结束后，避免完成截图时整个应用退出。
- 截图选区预览关闭位图插值，Retina 屏幕按原始像素显示，不再出现整块选区被平滑成高斯模糊的观感。

## 0.4.3 - 2026-08-13

### Fixed

- 将点击上传后的选区裁剪、红框/箭头合成和 PNG/JPEG 编码从 AppKit 全局绘图上下文迁移到独立 Core Graphics/ImageIO 渲染器，避免无法被 Swift `do/catch` 捕获的绘图异常。
- 大尺寸渲染改到后台任务；开始选区后立即释放其他显示器截图，降低 Retina、多屏场景的内存峰值。
- 服务地址、Token 与快捷键固定写入 `~/Library/Application Support/PocketIMGShot/settings.json`（目录 `0700`、文件 `0600`），不再依赖 `UserDefaults` 域或 App 签名身份。
- 截图生成与上传失败现在显示错误窗口，并将不含 Token/URL 的阶段日志写入 `~/Library/Logs/PocketIMGShot.log`。

## 0.4.2 - 2026-08-13

### Fixed

- 撤回 macOS 系统内容共享选择器，恢复 `F2` 后直接冻结所有屏幕并拖拽选择区域的截图流程。
- 移除 macOS 客户端的 Keychain 访问和 App Sandbox；服务地址、Token 与快捷键改存当前用户的应用偏好，替换 `.app` 后继续保留。
- 捕获仍通过单帧截图 API 完成并显式设置 `capturesAudio = false`，不创建屏幕共享流、不录制音频。

## 0.4.1 - 2026-08-13

### Fixed

- 修复截图工具栏图标被错误着色为实心方块的问题，改为不依赖字体的矢量路径绘制。
- 首次截取显示器时改用 macOS 系统内容选择器，避免触发“绕过系统私人窗口选择器并访问屏幕和音频”的警告以及权限变更后的进程退出。
- 屏幕捕获继续显式关闭音频；同一运行周期内会复用已授权显示器，鼠标切到其他显示器时再请求选择。

## 0.4.0 - 2026-08-13

### Added

- 增加原生 macOS 14+ 菜单栏截图客户端：默认 `F2` 全局快捷键并支持用户录制其他组合。
- 支持多显示器区域截图、红框和箭头标注、撤销与取消；选区工具栏最右侧固定为上传按钮。
- 客户端复用现有 Token/Session 和图片上传 API，成功后自动把公开 URL 写入剪贴板；Token 保存在 macOS 钥匙串。

## 0.3.2 - 2026-08-12

### Changed

- 删除右下角上传结果历史面板；上传期间只在页面顶部显示非阻塞全局进度条，浏览器优化或后端处理阶段使用不确定进度动画，完成后自动消失。
- 上传成功继续使用简短 Toast 并保留单图自动复制；失败改为 Toast 汇总，不在页面保留任务记录。
- 图片列表只保留“7 天”和“全部”两个筛选，默认显示最近 7 天。
- 图片按 `Asia/Shanghai` 日期倒序分组为时间线，每个日期标题下使用统一 4:3 缩略图网格，点击仍预览完整比例原图。
- 卡片复制、删除图标改为图片右上角浮层；桌面悬停或键盘聚焦时显示，触屏设备常显。
- 多选操作从页面底部移入顶部栏，并在多选期间替换日期范围选择器。
- Android 默认版本更新为 `0.3.2`（versionCode 10）。

## 0.3.1 - 2026-08-12

### Added

- 后端增加内存滚动窗口限流，所有上传和公开图片额度使用稳定空间 ID 隔离，轮换 Token 不会重置额度。
- 每个空间每小时最多接受 500 次上传尝试且同时最多处理 2 个；全部公开原图合计每小时最多响应 500 次。
- 每个空间的单张缩略图每分钟最多响应 5 次、每小时最多响应 10 次；全部缩略图合计每小时最多响应 2000 次。
- Token 换 Session 增加来源、全局和有效空间三层令牌桶；Caddy 可通过仅在回环代理连接上受信的专用请求头传递客户端 IP。

### Security

- 所有限流拒绝统一返回不可缓存的 `429 Too Many Requests` 和 `Retry-After`，不向前端暴露额度计数。
- 限流日志只记录类别、空间 ID、图片 ID 或来源地址并按分钟抑制重复项，不记录 Token 或 Session Cookie。

### Changed

- Android 默认版本更新为 `0.3.1`（versionCode 9）。

## 0.3.0 - 2026-08-12

### Added

- Android App 可直接维护 SSH 反向隧道，配置公网 HTTPS 地址、SSH 入口、受限用户、远端回环端口与服务器主机密钥 SHA-256 指纹。
- 首次启用时在 App 私有目录生成独立 Ed25519 设备密钥；App 只提供公钥复制入口，私钥固定使用 `0600` 且禁止备份。
- App 展示隧道连接/重试状态；Go 子进程使用 keepalive 和有上限退避自动恢复断开的 SSH 连接。
- 增加“隧道启用后仅允许本机访问 HTTP”选项，完成迁移后可让服务只监听 `127.0.0.1`。

### Security

- SSH 服务器主机密钥必须显式固定，不支持首次连接自动信任。
- 远端监听和本机转发目标均强制使用数字回环地址，避免配置错误把 PocketIMG 变成通用网络代理。
- 实际 SSH 端点、授权公钥、私钥和腾讯云配置不进入应用仓库；部署配置由独立运维仓库维护。

### Changed

- 启用 App 隧道时自动启用外部 HTTPS Cookie 模式。
- Android 默认版本更新为 `0.3.0`（versionCode 8）。

## 0.2.1 - 2026-08-12

### Changed

- 桌面端瀑布流改为 190 px 小图密集布局，卡片操作区同步收紧；移动端仍保持两列触屏布局。
- 上传状态按最新记录倒序展示，最多保留并显示最近 5 条；每条增加图片名称和上传时间。
- 移除上传结果弹窗的批量复制按钮；单图上传完成后的自动复制及图库卡片复制功能保持不变。
- Android 默认版本更新为 `0.2.1`（versionCode 7）。

## 0.2.0 - 2026-08-12

### Added

- 增加管理员用户管理：主空间管理员可在 Web 页面创建空间，后端生成一次性展示的 256-bit Token；新 Token 即对应一个独立图库用户。
- 每个用户默认拥有 10 GiB 实际存储配额，配额计算包含全尺寸图片与已生成缩略图。
- 每个用户默认保留图片 90 天；后台任务按创建时间永久删除过期图片、缩略图与元数据。

### Changed

- 动态创建的用户只在 SQLite 保存 Token SHA-256 指纹，不把明文 Token 写回配置文件；服务重启后仍可登录。
- 缩略图任务按固定批次执行，失败后指数退避，连续 5 次失败后停止重试并回退到全图，避免损坏文件永久消耗 CPU。
- 过期 Session 改为后台周期清理；HTTP 请求头上限显式收紧为 64 KiB。
- 页面响应增加 CSP、点击劫持保护和最小浏览器权限策略。
- Android 默认版本更新为 `0.2.0`（versionCode 6）。

## 0.1.4 - 2026-08-12

### Added

- 缩略图改由单独的持久化后台任务生成；服务启动时会自动补做数据库中尚未完成的缩略图。
- 上传进度在请求体发送完成后显示“处理中”，只在后端正式返回可访问 URL 后显示成功。

### Changed

- 安全的浏览器 WebP 完成校验、原子落盘和元数据写入后立即响应，不再等待缩略图解码与编码。
- 新上传图片在缩略图就绪前临时使用公开原图展示，后续图库请求自动使用缩略图。
- Android 默认版本更新为 `0.1.4`（versionCode 5）。

### Fixed

- 日期范围切换时保留现有瀑布流位置，刷新指示器不再占据文档流并把图片整体向下推。
- 缩略图后台生成与永久删除串行化，避免删除过程中重新产生孤立缩略图。

## 0.1.3 - 2026-08-12

### Added

- Android 前台服务增加 Go 子进程退出监听和周期健康检查，异常时自动清理旧进程并恢复后端。
- 恢复失败使用最高 5 分钟的有上限退避，稳定运行 5 分钟后自动重置，避免崩溃循环。

### Changed

- Android 默认版本更新为 `0.1.3`（versionCode 4）。

## 0.1.2 - 2026-08-12

### Added

- Android App 增加“局域网 HTTP / 外部 HTTPS”访问模式；HTTPS 模式启用 Secure Session Cookie 和更长的上传超时。
- 增加 `BOOT_COMPLETED` 与 `MY_PACKAGE_REPLACED` 恢复入口，并为启动失败增加有限退避重试。
- Android App 增加“后台运行设置”入口；重新打开 App 时会恢复用户原先要求保持运行、但被系统终止的服务。
- 增加 Root 开发实例到 App 私有目录的可回滚迁移脚本和操作文档。
- 增加通用反向代理契约，不包含具体域名、凭据、DNS 或隧道配置。
- Go 服务增加 `PIH_READ_TIMEOUT`、`PIH_WRITE_TIMEOUT` duration 配置。

### Changed

- App 从 Token 文件识别并保留既有空间 ID，不再强制把迁入数据归到 `mobile`。
- Origin 校验只使用代理保留的原始 `Host`，不再无条件信任 `X-Forwarded-Host`。
- Android 默认版本更新为 `0.1.2`（versionCode 3）。

### Validation fixes

- MIUI 未授权“自启动”时，划掉任务卡会把应用标记为系统强停；安装文档补充无需 Root 的授权步骤，并在真机授权后验证任务卡移除不再中断服务。
- Android 界面与前台服务不再拆分进程，避免 `SharedPreferences` 的访问模式、端口和期望运行状态在跨进程缓存中不同步；Go 服务仍保持为独立子进程。
- Android 真机的 `adb shell su -c` 不会可靠保留多行参数边界；迁移脚本改用编码后的单一 root shell 输入，防止后续命令意外退回普通 shell 权限。
- 迁移必须同时复制数据库、对象、缩略图、Token 和空间 ID；只切换二进制会造成历史图库不可见。
- 公网入口启用前必须从当前 Root 开发运行时切换到唯一的 App 运行时，避免两个服务争用端口或使用不同数据目录。

### Validated

- Root 实例无损迁移到 Android App；既有图库条目、空间 ID 和历史图片 SHA-256 保持一致。
- `img.901200.xyz` 的 IPv4/IPv6、HTTPS、DNS-only 记录、独立受限反向 SSH 隧道和 Caddy 代理链路通过端到端验证。
- 公网鉴权、Secure/HttpOnly/SameSite=Strict Session Cookie、Origin 防护、WebP 上传、公开读取和永久删除均通过。
- 真机开机并完成首次解锁后，App 服务与公网入口自动恢复为 200；按用户要求未再执行第二次重启。
- 公网并发验证无错误：健康接口 400 次请求、并发 40；公开图片 200 次请求、并发 20；上传并转码 20 张、并发 5，随后全部永久删除。
