# Tasks 001

## 分析

- [x] 核对后端 Token 来源、管理员选择和缺失配置行为
- [x] 核对 Linux、Docker 与飞牛部署差异
- [x] 明确首次安装、复用、失败保护和面向用户输出规则
- [x] 编写规格与分阶段实现建议

## 后端初始化

- [x] 增加安装器内部使用的初始化子命令
- [x] 复用并完善配置校验，正常启动只读取已有配置、不生成凭证
- [x] 实现 32 字节密码学随机 Token 和稳定管理员空间
- [x] 实现受限权限、不覆盖写入、持久化与互斥
- [x] 实现已有数据但凭证缺失时的保护
- [x] 实现供安装器捕获并直接展示的结果文本

## 安装入口

- [x] Linux 脚本自动初始化、配置服务并检查健康状态
- [x] Compose 脚本自动初始化同一持久化卷并启动服务
- [x] 处理既有手工配置，避免多来源冲突及覆盖部署配置
- [x] 安装成功直接展示地址、管理员、Token 和保存位置
- [x] 失败时保留凭证并明确报告失败；重试复用原配置

## 验证与文档

- [x] 空目录初始化后使用输出 Token 建立实际后端登录会话
- [x] 验证重复安装和容器重建复用 Token；已有空间归属测试回归通过
- [x] 覆盖自定义配置、旧单 Token 和多来源冲突
- [x] 覆盖已有数据、损坏配置、权限与写入失败
- [x] 覆盖随机数失败后重试、并发及模拟中断残留文件保护
- [x] 在目标 Linux 主机验证完整 systemd 安装、服务身份读取及版本升级
- [x] 验证 Docker 非 root、只读根文件系统及卷持久化
- [x] 验证普通服务日志不输出 Token
- [x] 回归飞牛免静态 Token 启动
- [x] 更新 README、Linux 和 Docker 部署及备份说明

## 验证范围

- Go 全量测试、初始化 race 检测、安装脚本 Bash 语法及 systemd unit 静态验证已通过。
- `bash scripts/test-docker-install.sh` 已通过：真实安装输出、管理员登录、文件与目录权限、重复安装、容器重建、日志无明文凭证。已接入容器 CI。
- Docker 默认 Go 模块代理在本机构建时超时，使用 `GOPROXY=https://goproxy.cn,direct` 构建参数完成验证；仓库默认代理未修改。
- Linux 安装器已在用户提供的阿里云 x86_64 主机完成真实 root/systemd 验收，使用回环端口 `18080`，未开放公网端口：首次安装直接输出 64 位 Token；`pocketimg` 服务身份能读取 `0600` 凭证；输出 Token 可建立会话并访问管理员 API。
- 实机重复安装、从 `bootstrap-test-1` 替换为 `bootstrap-test-2` 构建、systemd 重启后，凭证文件校验和不变，原 Token 仍能访问管理员 API；journal 和静默升级输出不包含 Token。升级验证覆盖二进制替换，不包含新数据库版本迁移。
- 实机停止服务并暂存凭证后重新安装，安装被拒绝、未生成新凭证、数据库校验和不变；恢复原凭证后健康检查通过。测试结束已停止服务并取消开机启动，安装文件与配置保留。
- 中断测试模拟残留文件与失败重试，不等同于实际断电及文件系统崩溃测试。
