# Tasks 003

- [x] 检查既有安装器、组件 Release 与远端 CI 状态
- [x] 实现 GitHub 下载入口、指定版本、仅下载模式
- [x] 实现校验和及压缩包结构验证
- [x] 增加同版本安装包生成与 Server 发布附件
- [x] 9 项下载器 fixture 测试通过并接入 CI/发布构建
- [x] 验证真实二进制打包、解压与初始化
- [x] 实现 `18746` 新安装默认端口和 `--port` 参数
- [x] Docker 自定义端口、重装保留映射和登录验收通过
- [x] Linux 实机修改端口、保留凭证和占用保护验收
- [x] 更新安装文档与 HTTP/HTTPS 边界
- [ ] 提交推送、通过新提交 CI 并发布兼容 Server Release
- [ ] 正式发布后验证公网一键下载与安装

远端已有提交 `e6789e9` 的 CI 已全部通过；本轮新增流程尚未推送，不将旧 CI 结果作为新改动的验证。

端口验收：用户提供的阿里云机器上改为自定义测试端口后健康检查通过，凭证校验和不变；再次安装不传端口仍使用该测试端口。用临时 HTTP 服务占用另一测试端口后，安装器拒绝该端口，部署配置未改变，原服务仍健康。测试后恢复原端口配置，并停止、禁用测试服务。Docker 实际安装也已验证显式端口及未传端口时复用原容器映射；Compose 默认展开为宿主机 `18746` 对应容器 `8080`。
