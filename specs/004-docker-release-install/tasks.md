# Tasks 004

- [x] 调整 Server/fnOS/全组件发布依赖
- [x] Docker 部署包固定摘要，添加 GitHub Docker 安装模式
- [x] 保留源码构建入口及已安装凭证/配置
- [x] Linux 下载器 9 项、Docker 下载/部署保护 15 项离线测试通过
- [x] 原 Docker 安装真实初始化、重装、登录与权限测试通过
- [x] 更新 README、Docker 和发布文档
- [x] 新镜像构建及真实部署包安装验证
- [x] 发布工作流静态检查（actionlint 1.7.7、YAML 解析）
- [x] 提交 `0ba77c5` 推送后 GitHub CI 全部通过
- [x] 经授权在阿里云安装 Docker，并完成隔离部署包实测
- [x] Server 0.5.3 发布工作流成功，提供六个附件及 amd64/arm64 镜像
- [x] 阿里云通过公开 GitHub 入口完成部署包下载、校验及 GHCR 拉取安装

本地实测发现跨阶段 COPY 未保留目标目录的 0700 权限，已用显式 `COPY --chmod=0700` 修复。新镜像部署包的初始化、Token 复用、端口保留、登录、容器重建、权限和日志脱敏测试通过；测试项目及数据卷已清理。amd64/arm64 双架构构建通过（ARM 为交叉构建，未做 ARM 实机运行）。

阿里云 <SERVER_IP> 为 Ubuntu 22.04 x86_64。用户授权后，通过系统软件源安装 Docker 29.1.3 和 Compose 2.40.3，Docker 保留运行并启用开机启动。导入本地构建的测试镜像，在独立 Compose 项目、数据卷和独立测试端口上验证部署包：自动凭证、0700/0600 权限、重装保留 Token/端口、容器重建后登录和日志脱敏全部通过。未登录上传与列表返回 401，登录后上传和列表成功，无 Cookie/Token 图片直链返回 200；该检查已加入持续集成的容器测试。

测试后移除测试容器、卷、网络、镜像和临时部署文件；原 PocketIMG systemd 服务保持 inactive，未更改原部署配置。新镜像尚未正式发布，此次是导入镜像后的真实部署包验收，不宣称完成新 Release 的 GitHub 下载/GHCR 拉取链路。

## 正式发布验收

随后经用户授权继续发布 `server-v0.5.3`（提交 `e691353`），发布工作流 `34087188568` 全部通过。镜像摘要为 `sha256:d6240ca568549127f45bf2a702cd2f3093a5dac8cebab9a9cdcab7897d294771`。

在阿里云直接执行公开 `install.sh --docker`，自动选择 0.5.3、校验部署包并从 GHCR 拉取摘要镜像成功；一次网络连接超时经安装器重试恢复。固定版本重装、默认端口 18746 保留、Token 校验和保持、权限、登录、图片访问边界及容器重建均通过。测试样张按 ID 删除，不保留演示数据。

按用户最新要求，新服务保留运行：`/opt/pocketimg-docker`、Compose 项目 `pocketimg`、数据卷 `pocketimg_pocketimg-data`，容器健康且使用 `unless-stopped` 重启策略。原 systemd 服务保持 inactive/disabled，旧数据和凭证未删除。本机 HTTP 健康检查通过；从外部访问 <SERVER_IP>:18746 超时，未据此宣称公网入站已开放，也未擅自改动安全组或防火墙。
