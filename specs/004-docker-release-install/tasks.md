# Tasks 004

- [x] 调整 Server/fnOS/全组件发布依赖
- [x] Docker 部署包固定摘要，添加 GitHub Docker 安装模式
- [x] 保留源码构建入口及已安装凭证/配置
- [x] Linux 下载器 9 项、Docker 下载/部署保护 15 项离线测试通过
- [x] 原 Docker 安装真实初始化、重装、登录与权限测试通过
- [x] 更新 README、Docker 和发布文档
- [x] 新镜像构建及真实部署包安装验证
- [x] 发布工作流静态检查（actionlint 1.7.7、YAML 解析）
- [x] 提交 `0ba77c5` 推送后 GitHub CI 全部通过
- [x] 在云测试主机安装 Docker，并完成隔离部署包实测
- [x] Server 0.5.3 发布工作流成功，提供六个附件及 amd64/arm64 镜像
- [x] 通过公开 GitHub 入口完成部署包下载、校验及 GHCR 拉取安装

本地实测发现跨阶段 COPY 未保留目标目录的 0700 权限，已用显式 `COPY --chmod=0700` 修复。新镜像部署包的初始化、Token 复用、端口保留、登录、容器重建、权限和日志脱敏测试通过；测试项目及数据卷已清理。amd64/arm64 双架构构建通过（ARM 为交叉构建，未做 ARM 实机运行）。

测试环境为 Ubuntu 22.04 x86_64、Docker 29.1.3 和 Compose 2.40.3。隔离部署包验收覆盖自动凭证、0700/0600 权限、重装保留 Token/端口、容器重建后登录和日志脱敏。未登录上传与列表返回 401，登录后上传和列表成功，无 Cookie/Token 图片直链返回 200；该检查已加入持续集成的容器测试。

隔离测试资源已清理。此阶段验证导入镜像后的部署包安装；GitHub 下载与 GHCR 拉取链路见下方正式发布验收。

## 正式发布验收

正式发布 `server-v0.5.3`（提交 `e691353`），发布工作流 `34087188568` 全部通过。镜像摘要为 `sha256:d6240ca568549127f45bf2a702cd2f3093a5dac8cebab9a9cdcab7897d294771`。

执行公开 `install.sh --docker`，自动选择 0.5.3、校验部署包并从 GHCR 拉取摘要镜像成功，网络连接超时后的重试恢复通过。固定版本重装、端口保留、凭证保持、权限、登录、图片访问边界及容器重建均通过。测试样张已清理。
