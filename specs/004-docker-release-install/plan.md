# Plan 004

1. 将多架构镜像发布移到 Server，保护已有版本，完成匿名拉取检查后生成 Docker 部署包。
2. 复用 GitHub 下载器的版本筛选、校验与解包逻辑，增加 Docker 模式及受管部署目录。
3. fnOS 改为消费同版本 Server 镜像；更新全组件依赖顺序。
4. 增加离线 fixture 与真实临时 Docker 项目测试，接入 CI；更新推荐命令及发布文档。
